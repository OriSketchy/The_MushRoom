using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    /// Set up tilt controls
    /// If you're completely out of the greater trigger zone then just exclude that from calculations
    /// If you're inside a larger trigger zone then calculate which smaller trigger you're nearer too and move in that direction
    /// If you're touching a smaller trigger just move in that direction
    /// 


}
